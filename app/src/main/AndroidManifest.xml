<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".JobsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jobs"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".BookmarkFragment"></activity>
        <activity android:name=".HomeFragment"></activity>
        <activity android:name=".JobsAdapter"></activity>
        <activity android:name=".TestApiService"></activity>
        <activity android:name=".Job"></activity>
        <activity android:name=".AppDatabase"></activity>
        <activity android:name=".BookMarkDao"></activity>
        <activity android:name=".BookMark"></activity>



    </application>





</manifest>